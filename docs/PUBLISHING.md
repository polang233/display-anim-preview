# Publishing

The official Blockbench repository receives only the compiled plugin, icon, about page,
changelog, and optional members file. This public repository contains the human-readable source,
tests, and reproducible build required for a bundled plugin.

Official metadata must link to this repository and must exactly match the metadata passed to
`Plugin.register()`.
